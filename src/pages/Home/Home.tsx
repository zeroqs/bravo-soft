import { Container } from '@mantine/core';

import { Header } from '@/widgets/Header/Header';

const Home = () => {
  return (
    <Container size={1420}>
      <Header />
      Home
    </Container>
  );
};

export default Home;
